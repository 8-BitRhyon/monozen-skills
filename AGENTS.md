# monozen-skills

Central repository for Monozen agent skills. Each subdirectory under `skills/`
is a standalone SKILL.md package following the Agent Skills convention.

Skills install via:
```bash
npx skills add 8-BitRhyon/monozen-skills --skill <name>
```
